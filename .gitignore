# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
dist/
build/
*.egg

# Virtual environments
.venv/
# FDA-granted venv that runs the MCP under Claude Desktop (see docs/FDA-SETUP).
# NOTE: .gitignore has no trailing-comment syntax -- a comment must be on its
# own line, never after the pattern, or it becomes part of the pattern.
.venv-fda/
venv/
env/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Testing
.pytest_cache/
.coverage
htmlcov/

# Distribution
*.tar.gz
*.whl

# VBW planning workflow (local planning artifacts — not tracked)
# Contains phase plans, summaries, verifications, UAT reports,
# milestone archives, and remediation rounds. Per-project state only.
.vbw-planning/

# Editor + tooling caches
.claude/
.git.bak/

# Backup git directory from history rewrite (safe to delete)
/.git.bak/
